"back" [htmlModel.canBack] { enabled: true; }
"back" [!htmlModel.canBack] { enabled: false; }

"forward" [htmlModel.canForward] { enabled: true; }
"forward" [!htmlModel.canForward] { enabled: false; }