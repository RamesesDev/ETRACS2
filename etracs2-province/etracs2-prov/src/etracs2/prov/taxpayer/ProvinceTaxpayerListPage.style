"selectedItem" [editing == true] {
    enabled:false;
}

"selectedItem" [editing == false] {
    enabled:true;
}

"search.textSearch" [editing == true] {
    enabled:false;
}

"search.textSearch" [editing == false] {
    enabled:true;
}