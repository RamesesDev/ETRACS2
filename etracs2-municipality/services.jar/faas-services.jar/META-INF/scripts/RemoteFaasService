import com.rameses.annotations.*;

public class RemoteFaasService {

	@Service("Var")
	def var;

	@ProxyMethod
	@Async(type="topic")
	public def post( params ) {
		
		if ( params.targetLgu == var.lgu_index ) {
			print "INVOKED >> " + params;
		}
		
	}

}
