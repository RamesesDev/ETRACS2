<workunit>
    <invokers>
        <invoker type="home.action" folderid="/menu/bp/txn" action="initApplication" caption="Approved Applications" permission="bppermit.approvedapplications" id="approvedapplications" target="window"/>
        <invoker type="home.action" folderid="/menu/bp/txn" action="initReleasePermit" caption="For Release Permits" permission="bppermit.forreleasepermits" id="forreleasepermits" target="window"/>
        <invoker type="home.action" folderid="/menu/bp/txn" action="initActivePermit" caption="Active Permits" permission="bppermit.activepermits" id="activepermits" target="window"/>
        <invoker type="home.action" folderid="/menu/bp/txn" caption="Retired Application" permission="bppermit.retire" id="retiredapplications" target="window"/>

        <invoker type="formActions" action="previewApplication" caption="View Application" visibleWhen="#{selectedItem != null}" icon="images/view24.png"/>
        <invoker type="formActions" action="previewAssessment" caption="View Assessment" visibleWhen="#{selectedItem != null}" icon="images/view24.png"/>
        <invoker type="formActions" action="open" caption="Open" visibleWhen="#{selectedItem != null and selectedItem.docstate == 'ACTIVE' }" icon="images/open24.png"/>
    </invokers>
    <code>
<![CDATA[

import com.rameses.rcp.common.*;
import com.rameses.rcp.annotations.*;
import com.rameses.osiris2.client.*;

class BusinessMgmtList
{
    @Binding
    def binding;
    
    def mode = 'VIEW';
    
    @Service('BusinessMgmtService')
    def svc;
    
    @Service('BPHtmlService')
    def htmlSvc;
    
    def txntype;
    def docstate;
    def searchText;
    
    void initApplication(){ docstate = 'APPROVED'; }
    void initReleasePermit(){ docstate = 'PERMIT_PENDING'; }
    void initActivePermit(){ docstate = 'ACTIVE'; }
    
    def selectedItem;
    def listHandler = [
        getRows: { return 25; },
        getColumns : {
            return [
                new Column(name:'docstate', caption:'Status'),
                new Column(name:'txnno', caption:'Application No.'),
                new Column(name:'tradename', caption:'Trade Name'),
                new Column(name:'businessaddress', caption:'Business Address' ),
                new Column(name:'taxpayername', caption:'Payer Name'),
                new Column(name:'taxpayeraddress', caption:'Payer Address')
            ];
        },
        
        fetchList : {
            return svc.getList( searchText, docstate, it );
        }
    ] as PageListModel;
    
    public def getHtml(){
        def html = "";
        if( selectedItem ) {
            if( !selectedItem.html ){
                try{ selectedItem.html = htmlSvc.getHtml( selectedItem.objid );
                }catch(e){ e.printStackTrace(); }
            }
            html = selectedItem.html;
        }
        
        if( html ) return html;
        return 'No selected item';
    }
    
    def previewApplication(){
        return InvokerUtil.lookupOpener('bp.applicationreport', [applicationid:selectedItem.objid]);
    }
    
    def previewAssessment(){
        return InvokerUtil.lookupOpener('bp.assessmentreport', [applicationid:selectedItem.objid]);
    }
    
    def retire(){ return InvokerUtil.lookupOpener('bp.retire', [applicationid: selectedItem.objid]);}
    
    def release(){
        def permit = svc.openPermit( selectedItem.objid );
        return InvokerUtil.lookupOpener('bp.releasepermit', [permit: permit]);
    }
    
    def open(){ return selectedItem.txntype.matches('RETIRE|RETIRELOB') ? retire() : release(); }
    void search(){ listHandler.load(); }
}

]]>        
        
    </code>
    <pages>
        <page template="etracs2.bp.application.BusinessMmgtPage"/>
    </pages>
</workunit>