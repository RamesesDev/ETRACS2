<workunit>
    <invokers>
        <invoker type="account:lookup.ngas" action="initNgas" caption="NGAS Account Lookup" target="popup" />
        <invoker type="account:lookup.sre" action="initSre" caption="SRE Account Lookup" target="popup" />
    </invokers>
    
    <code>
        
<![CDATA[

import com.rameses.rcp.common.*;
import com.rameses.rcp.annotations.*;

class LookupAccountController 
{
    @Service("AccountService")
    def svc;
    
    def onselect;
    def searchText;
    def selected;
    def chartType;
    
    void initNgas() {
        chartType = 'NGAS';
    }
    
    void initSre() {
        chartType = 'SRE';
    }
    
    def ok() {
        if( onselect ) {
            onselect( selected );
        }
        return "_close";
    }
    
    def listHandler = [
        getRows    : { return 20; },
        getColumns : {
            return [
                new Column(name:"acctcode", caption:"Code", width:70),
                new Column(name:"accttitle", caption:"Title", width:250),
            ]
        },
        fetchList : { 
                def list = svc.getAccountList(searchText, chartType, it);
                if( list ) return list;
                throw new Exception("Record not found.");
	},
    ] as PageListModel;
}  


]]>

    </code>
    
    <pages>
        <page template="etracs2.tc.LookupPage"/>
    </pages>
</workunit>