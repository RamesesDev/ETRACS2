<?xml version="1.0" encoding="UTF-8"?>
<workunit>
    <invokers>
        <invoker type="liquidation:report" caption="Liquidation Report By Chart And Fund"/>
        <invoker type="formActions" action="back" caption="Back" icon="images/cancel24.png"/>
    </invokers>

    <code lang="groovy">
        <![CDATA[
        import com.rameses.rcp.common.*;
        import com.rameses.rcp.annotations.*;
        import com.rameses.osiris2.client.*;
        import com.rameses.osiris2.reports.*

        public class LiquidationReportByFund {

            @Service("ReportParameterService")
            def svcParam;

            @Service("LiquidationReportService")
            def svc;
            
            def fund;
            def liquidation;
            
            def param = [:];
            
            def reportModel = [
                getReportName : { return 'liquidation/reportc'; },
                getReportData : { return [liquidationData] },
                getParameters : { return svcParam.getStandardParameter( 'system' )+param; }
            ] as ServerReportModel;
            
            public def getReport(){
                reportModel.viewReport();
                return reportModel.report;
            }
            
            public def getLiquidationData(){
                if( !fund ) fund = fundList[0];
                if( !chart ) chart = chartList[0];
                
                param.put('FUND', fund.fundname );
                param.put('CHART', chart );
                
                return svc.buildReportCData( liquidation.objid, chart , fund.objid );
            }
            
            def funds;
            public def getFundList(){
                if( !funds ) this.funds = svc.fundList;
                return funds;
            }
            
            def chart;
            public def getChartList(){
                return ['NGAS','SRE'];
            }
            
            def back(){
                return "_close";
            }

        }
        ]]>
    </code>
    
    <pages>
        <page template="etracs2.tc.liquidation.LiquidationReportBPage" />
    </pages>
</workunit>
