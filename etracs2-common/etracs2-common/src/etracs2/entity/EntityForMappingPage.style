"selectedItem|searchText" [mode == 'view'] {
    enabled:true;
}

"selectedItem|searchText" [mode != 'view'] {
    enabled:false;
}


"selectedMapping|mapping" [mode == 'view'] {
    enabled:false;
}

"selectedMapping|mapping" [mode != 'view'] {
    enabled:true;
}
